# Postgres event store (billing events, MRR movements, recovery attempts)
DATABASE_URL="postgresql://churnwise:churnwise@localhost:5432/churnwise?schema=public"

# Shared secret used to sign billing webhooks (HMAC-SHA256 of "<timestamp>.<raw body>")
BILLING_WEBHOOK_SECRET="whsec_replace_me"
